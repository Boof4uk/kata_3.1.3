@ModelAtribute - нужна для того, чтобы передать данные с формы в модель, при условии, что поля формы
соответствуют полям модели. Позволяет получать данные из запроса и заполнять объект, который вы
передаете методу контроллера.
Это полезно, когда вы хотите получить данные от пользователя через форму. Делается в колаборации с <th:object>

Model model - контейнер, в который мы можем положить объекты из java и передать их в html с помощью ThLe

Этот тег <form> в HTML используется для создания HTML-формы,
которая позволяет пользователям отправлять данные на сервер

th:method="GET": Это атрибут th:method, который определяет метод HTTP-запроса, который будет использоваться при отправке формы. В данном случае, метод GET означает,
что данные формы будут прикреплены к URL, и запрос будет выполняться в режиме чтения данных с сервера.

th:action="@{/admin/edit/{id}(id=${user.getId()})}": Это атрибут th:action,
который определяет URL, на который будет отправлен запрос при отправке формы.
/admin/edit/{id} - это часть URL, которая будет фиксированной и представляет собой путь к ресурсу редактирования.
(id=${user.getId()}) - это Thymeleaf-выражение, которое вставляет значение идентификатора пользователя user.getId() в место {id} в URL.
Это позволяет динамически формировать URL на основе значения user.getId().